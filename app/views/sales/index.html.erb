<div class="container clearfix">
    <div class="float-left">
        <h2> Sales </h2>
    </div>
    
    <div class="float-right">
        <a class="btn btn-sm btn-secondary <%= params[:sort] == "amount" ? "active": nil%>" href="?sort=amount">Amount</a>
        <a class="btn btn-sm btn-secondary mr-3 <%= params[:sort] == "recent" ? "active": nil%> <%= params[:sort] == nil ? "active": nil%>" href="?sort=recent">Recent</a>
        <%= link_to "New Sale", new_sale_path, :class=>"btn btn-primary" %>
    </div>
</div>

<div class="container">
    <hr />
</div>

<div class="container">
    <div class="">
        <%# render 'sales/partials/filter.html.erb' %>
        <%= render 'sales/partials/sales.html.erb', locals:{sales: @sales}%>
    </div>
</div>
